package bellosqueues.spacexplosions.services;

/**
 * Our Map Logic | Binds all the other models together in the same place
 */
public class MapService {

    //Defines Grid size
    //Connects Position(logic) to Cell(pixel)

}
